version: '3.9'
services:
  task-manger-app:
    image: postgres
    restart: always
    container_name: task-manger-app
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: example
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres

volumes:
  postgres:
